<?php

// Broadcasting channels disabled for production
// Re-enable when Reverb/Pusher is configured

// Broadcast::channel('App.Models.User.{id}', function ($user, $id) {
//     return (int) $user->id === (int) $id;
// });
