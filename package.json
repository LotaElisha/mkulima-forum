{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && mkdir -p dist && cp -r public/build/* dist/ && php -r \"require 'vendor/autoload.php'; \\$app = require 'bootstrap/app.php'; \\$kernel = \\$app->make(Illuminate\\Contracts\\Console\\Kernel::class); \\$kernel->bootstrap(); echo view('landing', ['settings' => []])->render();\" > dist/index.html",
        "dev": "vite"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-echo": "^2.4.0",
        "laravel-vite-plugin": "^1.2.0",
        "postcss": "^8.4.47",
        "pusher-js": "^8.6.0",
        "tailwindcss": "^3.4.13",
        "vite": "^6.0.11"
    }
}
